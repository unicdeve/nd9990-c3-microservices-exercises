apiVersion: v1
kind: Service
metadata:
  name: udagram-api-feed-srv
  labels:
    run: udagram-api-feed-srv
spec:
  ports:
    - port: 80
      protocol: TCP
  selector:
    run: udagram-api-feed-srv
